    <!-- Navigation -->
    <nav class="fixed w-full z-50 transition-all duration-300 glass-nav <%= typeof transparentNav !== 'undefined' && transparentNav ? '' : 'bg-emerald-950 shadow-xl' %>">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20 items-center">
                <a href="/" class="flex-shrink-0 flex items-center space-x-3 group">
                    <div class="w-12 h-12 bg-gold-500 rounded-full flex items-center justify-center text-white text-2xl group-hover:bg-gold-400 transition-colors">
                        <i class="fa-solid fa-mosque"></i>
                    </div>
                    <span class="text-white font-serif text-xl font-bold tracking-tight group-hover:text-gold-400 transition-colors">E G Community Centre</span>
                </a>
                
                <!-- Desktop Menu -->
                <div class="hidden md:flex space-x-8 items-center">
                    <a href="/" class="<%= page === 'home' ? 'text-gold-400 border-b-2 border-gold-400' : 'text-gray-200 hover:text-gold-400 transition-colors' %> px-1 pt-1 text-sm font-medium">Home</a>
                    <a href="/about" class="<%= page === 'about' ? 'text-gold-400 border-b-2 border-gold-400' : 'text-gray-200 hover:text-gold-400 transition-colors' %> px-1 pt-1 text-sm font-medium">About Us</a>
                    <a href="/events" class="<%= page === 'events' ? 'text-gold-400 border-b-2 border-gold-400' : 'text-gray-200 hover:text-gold-400 transition-colors' %> px-1 pt-1 text-sm font-medium">Events</a>
                    <a href="/contact" class="btn-gold">Contact Us</a>
                </div>

                <!-- Mobile menu button -->
                <div class="md:hidden flex items-center">
                    <button id="menu-btn" aria-label="Toggle mobile menu" class="text-white hover:text-gold-400 focus:outline-none">
                        <i class="fa-solid fa-bars text-2xl" aria-hidden="true"></i>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="hidden md:hidden bg-emerald-950 px-4 pt-2 pb-6 space-y-2 border-t border-emerald-800">
            <a href="/" class="block <%= page === 'home' ? 'text-gold-400' : 'text-gray-200 hover:text-gold-400' %> py-2 text-base font-medium">Home</a>
            <a href="/about" class="block <%= page === 'about' ? 'text-gold-400' : 'text-gray-200 hover:text-gold-400' %> py-2 text-base font-medium">About Us</a>
            <a href="/events" class="block <%= page === 'events' ? 'text-gold-400' : 'text-gray-200 hover:text-gold-400' %> py-2 text-base font-medium">Events</a>
            <a href="/contact" class="block text-center btn-gold mt-4">Contact Us</a>
        </div>
    </nav>
